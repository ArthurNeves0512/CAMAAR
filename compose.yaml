services:
  db:
    image: postgres:latest
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=camaar
      - POSTGRES_PASSWORD=camaar
      - POSTGRES_DB=CAMAAR_development
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
